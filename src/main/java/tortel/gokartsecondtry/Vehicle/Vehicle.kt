package tortel.gokartsecondtry.Vehicle



abstract class Vehicle : org.bukkit.entity.Horse {
    /*
    fun Vehicle(loc : Location) {
        super((loc.getWorld() as CraftWorld).handle)
        this.setPosition(loc.getX(), loc.getY(), loc.getZ())
    }

     */
}